import 'package:play_app/data/local_storage/moor_db/tables.dart';

class DBRepository {
  // final dbProvider = PlayerDatabase();

  // Future insertTeam(Team team) => dbProvider.insertTeam(team);
  // Future<List<Team>> extractTeam() => dbProvider.extractTeam();
}
